---
title: "Robin Hill School Book 1 <One Hundred Days(Plus One)>"
description: "로빈 힐 스쿨 1권 <백 번째 날(그리고 또 하루)>"
---

{/* ────────────────────────────────────────────────
    🎵 YouTube 스타일 챕터 오디오 플레이어
    ─────────────────────────────────────────────────
    사용법:
    1. AUDIO_SRC  → GitHub Raw URL로 교체
    2. chapters   → 본인 곡의 타임스탬프로 수정
    3. TOTAL      → 전체 재생 시간(초)으로 수정
    ──────────────────────────────────────────────── */}

<div id="__apl_root__">

<style>{`
  #__apl_root__ { font-family: inherit; }
  .apl { background: var(--card-background, #fff); border: 1px solid var(--border-color, #e5e7eb); border-radius: 12px; padding: 20px; max-width: 680px; }
  .apl-title { font-size: 15px; font-weight: 600; margin: 0 0 4px; color: var(--heading-color, #111); }
  .apl-meta  { font-size: 12px; color: var(--muted-foreground, #6b7280); margin: 0 0 16px; }

  /* ── 프로그레스 바 ── */
  .apl-pw  { position: relative; height: 30px; cursor: pointer; display: flex; align-items: center; }
  .apl-trk { position: relative; width: 100%; height: 4px; background: rgba(128,128,128,.2); border-radius: 2px; transition: height .1s; }
  .apl-pw:hover .apl-trk { height: 6px; }
  .apl-cseg { position: absolute; top: 0; height: 100%; border-radius: 2px; }
  .apl-fill { position: absolute; left: 0; top: 0; height: 100%; background: #ff0000; border-radius: 2px 0 0 2px; pointer-events: none; }
  .apl-thumb{ position: absolute; top: 50%; transform: translate(-50%,-50%); width: 13px; height: 13px; background: #fff; border: 2px solid #ff0000; border-radius: 50%; pointer-events: none; opacity: 0; transition: opacity .1s; box-shadow: 0 1px 4px rgba(0,0,0,.25); }
  .apl-pw:hover .apl-thumb { opacity: 1; }
  .apl-sep  { position: absolute; top: 50%; transform: translateY(-50%); width: 2px; height: 140%; background: var(--card-background, #fff); pointer-events: none; z-index: 2; }
  .apl-tip  { position: absolute; bottom: calc(100% + 10px); transform: translateX(-50%); background: rgba(0,0,0,.82); color: #fff; font-size: 11px; white-space: nowrap; padding: 4px 8px; border-radius: 4px; pointer-events: none; opacity: 0; transition: opacity .1s; z-index: 10; }

  /* ── 컨트롤 ── */
  .apl-ctrl { display: flex; align-items: center; gap: 12px; margin-top: 12px; }
  .apl-pbtn { width: 36px; height: 36px; border: 1px solid rgba(128,128,128,.3); border-radius: 50%; background: transparent; cursor: pointer; display: flex; align-items: center; justify-content: center; color: inherit; flex-shrink: 0; transition: background .12s; }
  .apl-pbtn:hover { background: rgba(128,128,128,.1); }
  .apl-time { font-size: 13px; color: var(--muted-foreground, #6b7280); font-variant-numeric: tabular-nums; }
  .apl-vw   { margin-left: auto; display: flex; align-items: center; gap: 8px; }
  .apl-vi   { font-size: 14px; cursor: pointer; }
  .apl-vr   { width: 72px; accent-color: #ff0000; }

  /* ── 챕터 목록 ── */
  .apl-clist { margin-top: 16px; border-top: 1px solid rgba(128,128,128,.15); padding-top: 12px; display: flex; flex-direction: column; gap: 2px; }
  .apl-crow  { display: flex; align-items: center; gap: 10px; padding: 7px 10px; border-radius: 6px; cursor: pointer; transition: background .1s; }
  .apl-crow:hover, .apl-crow.apl-active { background: rgba(128,128,128,.1); }
  .apl-cdot  { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
  .apl-cts   { font-size: 12px; color: #ff0000; min-width: 38px; font-weight: 600; font-variant-numeric: tabular-nums; }
  .apl-cname { font-size: 13px; flex: 1; }
  .apl-cdur  { font-size: 11px; color: var(--muted-foreground, #6b7280); margin-left: auto; }
`}</style>

<div className="apl">
  <p className="apl-title">트랙 제목을 여기에 입력하세요</p>
  <p className="apl-meta">아티스트 · 앨범 · 2025</p>

  <div className="apl-pw" id="apl-pw">
    <div className="apl-trk" id="apl-trk">
      <div className="apl-fill"  id="apl-fill"  style={{width: "0%"}} />
      <div className="apl-thumb" id="apl-thumb" style={{left:  "0%"}} />
      <div className="apl-tip"   id="apl-tip" />
    </div>
  </div>

  <div className="apl-ctrl">
    <button className="apl-pbtn" id="apl-pbtn" aria-label="재생/일시정지">
      <svg id="apl-ico" width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
        <polygon points="3,1 14,8 3,15" />
      </svg>
    </button>
    <span className="apl-time">
      <span id="apl-cur">0:00</span>
      {" / "}
      <span id="apl-dur">0:00</span>
    </span>
    <div className="apl-vw">
      <span className="apl-vi" id="apl-vi">🔊</span>
      <input type="range" className="apl-vr" id="apl-vr" min="0" max="1" step="0.01" defaultValue="1" />
    </div>
  </div>

  <div className="apl-clist" id="apl-clist" />
</div>

<audio id="apl-audio" preload="metadata" />

</div>

<script dangerouslySetInnerHTML={{ __html: `
(function() {

  /* ════════════════════════════════════════
     ✏️  여기만 수정하세요
  ════════════════════════════════════════ */

  // GitHub Raw URL 형식:
  // https://raw.githubusercontent.com/유저명/레포명/브랜치명/파일경로.mp3
  var AUDIO_SRC = "https://github.com/Longtail-team/audio-files-test/raw/refs/heads/main/Robin%20Hill%20School%20Book%201.mp3";

  // 전체 길이(초) — 오디오가 로드되면 자동으로 덮어씀
  var TOTAL = 377;

  // 챕터 배열: { name, start(초), color }
  var chapters = [
    { name: "", start: 0, color: "#ff0000" },
  ];

  /* ════════════════════════════════════════
     ⚙️  아래는 수정 불필요
  ════════════════════════════════════════ */

  function fmt(s) {
    s = Math.floor(s);
    return Math.floor(s / 60) + ":" + (s % 60 < 10 ? "0" : "") + (s % 60);
  }

  function getCurrentChapter(t) {
    var idx = 0;
    for (var i = 0; i < chapters.length; i++) {
      if (t >= chapters[i].start) idx = i;
    }
    return idx;
  }

  function init() {
    var audio  = document.getElementById("apl-audio");
    var fill   = document.getElementById("apl-fill");
    var thumb  = document.getElementById("apl-thumb");
    var tip    = document.getElementById("apl-tip");
    var trk    = document.getElementById("apl-trk");
    var pw     = document.getElementById("apl-pw");
    var pbtn   = document.getElementById("apl-pbtn");
    var curEl  = document.getElementById("apl-cur");
    var durEl  = document.getElementById("apl-dur");
    var vr     = document.getElementById("apl-vr");
    var vi     = document.getElementById("apl-vi");
    var clist  = document.getElementById("apl-clist");

    if (!audio || !fill) return;  // DOM 아직 없으면 재시도
    audio.src = AUDIO_SRC;

    /* 오디오 메타데이터 로드 후 길이 갱신 */
    audio.addEventListener("loadedmetadata", function() {
      TOTAL = audio.duration || TOTAL;
      durEl.textContent = fmt(TOTAL);
      buildSegments();
    });

    durEl.textContent = fmt(TOTAL);

    /* ── 챕터 세그먼트 & 구분선 ── */
    function buildSegments() {
      /* 기존 세그먼트 제거 */
      trk.querySelectorAll(".apl-cseg, .apl-sep").forEach(function(el){ el.remove(); });

      chapters.forEach(function(ch, i) {
        var next  = chapters[i + 1] ? chapters[i + 1].start : TOTAL;
        var wPct  = ((next - ch.start) / TOTAL * 100).toFixed(3) + "%";
        var lPct  = (ch.start / TOTAL * 100).toFixed(3) + "%";

        var seg = document.createElement("div");
        seg.className = "apl-cseg";
        seg.style.cssText = "left:" + lPct + ";width:" + wPct + ";background:" + ch.color + ";opacity:.7";
        trk.appendChild(seg);

        if (i > 0) {
          var sep = document.createElement("div");
          sep.className = "apl-sep";
          sep.style.left = lPct;
          trk.appendChild(sep);
        }
      });
    }

    /* ── 챕터 목록 ── */
    chapters.forEach(function(ch, i) {
      var next = chapters[i + 1] ? chapters[i + 1].start : TOTAL;
      var row = document.createElement("div");
      row.className = "apl-crow";
      row.id = "apl-ch-" + i;
      row.innerHTML =
        '<div class="apl-cdot" style="background:' + ch.color + '"></div>' +
        '<span class="apl-cts">' + fmt(ch.start) + '</span>' +
        '<span class="apl-cname">' + ch.name + '</span>' +
        '<span class="apl-cdur">' + fmt(next - ch.start) + '</span>';
      row.addEventListener("click", function() {
        audio.currentTime = ch.start;
        if (audio.paused) audio.play();
      });
      clist.appendChild(row);
    });

    buildSegments();

    /* ── 진행률 업데이트 ── */
    function updateProgress(t) {
      var pct = (t / TOTAL * 100).toFixed(3) + "%";
      fill.style.width  = pct;
      thumb.style.left  = pct;
      curEl.textContent = fmt(t);
      var ci = getCurrentChapter(t);
      document.querySelectorAll(".apl-crow").forEach(function(r, i) {
        r.classList.toggle("apl-active", i === ci);
      });
    }

    audio.addEventListener("timeupdate", function() {
      updateProgress(audio.currentTime);
    });

    /* ── 재생/일시정지 ── */
    function setPlayIcon(playing) {
      var svg = document.getElementById("apl-ico");
      var ns  = "http://www.w3.org/2000/svg";
      svg.innerHTML = "";
      if (playing) {
        var r1 = document.createElementNS(ns, "rect");
        r1.setAttribute("x", "2");  r1.setAttribute("y", "2");
        r1.setAttribute("width", "4"); r1.setAttribute("height", "12"); r1.setAttribute("rx", "1");
        var r2 = document.createElementNS(ns, "rect");
        r2.setAttribute("x", "10"); r2.setAttribute("y", "2");
        r2.setAttribute("width", "4"); r2.setAttribute("height", "12"); r2.setAttribute("rx", "1");
        svg.appendChild(r1); svg.appendChild(r2);
      } else {
        var poly = document.createElementNS(ns, "polygon");
        poly.setAttribute("points", "3,1 14,8 3,15");
        svg.appendChild(poly);
      }
    }
    pbtn.addEventListener("click", function() {
      if (audio.paused) { audio.play(); setPlayIcon(true); }
      else              { audio.pause(); setPlayIcon(false); }
    });
    audio.addEventListener("play",  function(){ setPlayIcon(true); });
    audio.addEventListener("pause", function(){ setPlayIcon(false); });
    audio.addEventListener("ended", function(){ setPlayIcon(false); updateProgress(0); });

    /* ── 클릭/드래그 seek ── */
    var dragging = false;
    function seekFromEvent(e) {
      var r = trk.getBoundingClientRect();
      var pct = Math.min(Math.max((e.clientX - r.left) / r.width, 0), 1);
      audio.currentTime = pct * TOTAL;
    }
    pw.addEventListener("mousedown", function(e){ dragging = true; seekFromEvent(e); });
    window.addEventListener("mouseup", function(){ dragging = false; });
    pw.addEventListener("mousemove", function(e) {
      var r = trk.getBoundingClientRect();
      var pct = Math.min(Math.max((e.clientX - r.left) / r.width, 0), 1);
      var t = pct * TOTAL;
      var ci = getCurrentChapter(t);
      tip.style.left    = (pct * 100) + "%";
      tip.textContent   = fmt(t) + " · " + chapters[ci].name;
      tip.style.opacity = "1";
      if (dragging) audio.currentTime = t;
    });
    pw.addEventListener("mouseleave", function(){ tip.style.opacity = "0"; dragging = false; });

    /* ── 볼륨 ── */
    vr.addEventListener("input", function() {
      audio.volume = parseFloat(vr.value);
      vi.textContent = vr.value == 0 ? "🔇" : vr.value < 0.5 ? "🔉" : "🔊";
    });

    updateProgress(0);
  }

  /* DOM이 준비된 뒤 실행 */
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", init);
  } else {
    init();
  }
})();
` }} />